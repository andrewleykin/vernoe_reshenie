.phone
	text-align right
	.icon
		size(26px)
		fill accentColor
		margin-right 10px
		margin-top -10px

.phone__link
	color accentColor
	font-size 24px
	line-height 28px
	font-family boldFont
	@extend .trs
	&:hover
		color darken(accentColor, 50%)

.phone__descr
	display block
	font-size 14px